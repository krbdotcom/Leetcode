int* buildArray(int* nums, int nSize, int* returnSize) {
    int i;
    int* ans=(int*)malloc(nSize*sizeof(int));
    for (i=0;i<nSize;i++)
        ans[i]=nums[nums[i]];
    *returnSize=nSize;
    return ans;
}
