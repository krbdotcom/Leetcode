int findNumbers(int* nums, int numsSize) {
    int i,j,c=0,s=0;
    for (i=0;i<numsSize;i++)
    {
        j=nums[i];
        while (j>0)
        {
            c=c+1;
            j=j/10;
        }
        if (c%2==0)
            s=s+1;
        c=0;
    }
    return s;
}
