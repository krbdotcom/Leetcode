int findClosest(int x, int y, int z) {
    int a,b;
    if ((z-x)<0)
        a=x-z;
    else
        a=z-x;
    if ((y-z)<0)
        b=z-y;
    else
        b=y-z;
    if (a<b)
        return 1;
    else if (a>b)
        return 2;
    else
        return 0;
}
