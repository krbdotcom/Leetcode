int singleNumber(int* nums, int numsSize) {
    int i,j,s=nums[0];
    if (numsSize==1)
        return s;
    else
        for (i=1;i<numsSize;i++)
        {
            s=s^nums[i];
        }
        return s;
    return 0;
}
