int alternatingSum(int* nums, int numsSize) {
    int i,s=0;
    for (i=0;i<numsSize;i++)
    {
        if (i%2==0)
            s=s+nums[i];
        else if (i%2!=0)
            s=s-nums[i];
    }
    return s;
}
