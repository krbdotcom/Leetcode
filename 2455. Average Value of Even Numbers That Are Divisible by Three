int averageValue(int* nums, int numsSize) {
    int i,j=0,s=0;
    for (i=0;i<numsSize;i++)
    {
        if ((nums[i]%2==0)&&(nums[i]%3==0))
        {
            s=s+nums[i];
            j=j+1;
        }
    }
    if (j!=0)
        return (s/j);
    else    
        return 0;
}
