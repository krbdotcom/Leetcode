long long removeZeros(long long n) {
    long long int x=0,ans=0;
    while (n>0)
    {
        if (n%10!=0)
            x=x*10+(n%10);
        n=n/10;
    }
    while (x>0)
    {
        ans=ans*10+(x%10);
        x=x/10;
    }
    return ans;
}
